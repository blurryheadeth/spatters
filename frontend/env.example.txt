# =============================================================================
# Spatters Frontend Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values
#
# IMPORTANT: Never commit .env.local to git!
# =============================================================================

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# Which network to use: 'sepolia' for testnet, 'mainnet' for production
NEXT_PUBLIC_NETWORK=mainnet

# Contract addresses
NEXT_PUBLIC_CONTRACT_ADDRESS=
NEXT_PUBLIC_SEPOLIA_CONTRACT_ADDRESS=
NEXT_PUBLIC_MAINNET_CONTRACT_ADDRESS=

# Generator contract address (SpattersGenerator)
NEXT_PUBLIC_GENERATOR_ADDRESS=

# Base URL for API endpoints (your deployed Vercel URL)
NEXT_PUBLIC_BASE_URL=https://spatters.art

# -----------------------------------------------------------------------------
# RPC Configuration
# -----------------------------------------------------------------------------
# Get API keys from Alchemy (https://www.alchemy.com)

# Server-side RPC (for API routes)
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
MAINNET_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Client-side RPC (public endpoints, used as fallback)
NEXT_PUBLIC_SEPOLIA_RPC_URL=https://eth-sepolia.public.blastapi.io
NEXT_PUBLIC_MAINNET_RPC_URL=https://eth.llamarpc.com

# -----------------------------------------------------------------------------
# Storage Configuration (Supabase)
# -----------------------------------------------------------------------------
# Create a project at https://supabase.com
# Go to Project Settings > API to get URL and service_role key

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_BUCKET=spatters-pixels

# -----------------------------------------------------------------------------
# WalletConnect Configuration
# -----------------------------------------------------------------------------
# Get a project ID at https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your-walletconnect-project-id

# -----------------------------------------------------------------------------
# GitHub Actions Configuration (for pixel generation)
# -----------------------------------------------------------------------------
# Create a fine-grained Personal Access Token at https://github.com/settings/tokens
# Required permissions: Contents (read/write)
NEXT_PUBLIC_GITHUB_OWNER=blurryheadeth
NEXT_PUBLIC_GITHUB_REPO=spatters
GITHUB_TOKEN=github_pat_xxxxxxxxxxxx
